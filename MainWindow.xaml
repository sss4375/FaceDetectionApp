<Window x:Class="FaceDetectionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FaceDetectionApp.ViewModels"
        xmlns:local="clr-namespace:FaceDetectionApp"
        mc:Ignorable="d"
        Title="Rudolph Nose Detector" Height="900" Width="1200">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 버튼 컨트롤 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <Button Content="웹캠 시작" Width="100" Command="{Binding StartWebcamCommand}" Height="30" />
            <Button Content="웹캠 중지" Width="100" Command="{Binding StopWebcamCommand}" Height="30" />
            <Button Content="파일 열기" Width="100" Command="{Binding LoadImageCommand}" Height="30"/>
        </StackPanel>

        <!-- 이미지 출력 영역 -->
        <Image Grid.Row="1" Stretch="Uniform"
               Source="{Binding CurrentFrame}" />

        

        <!-- 상태 메시지 -->
        <TextBlock Grid.Row="2" Margin="20" VerticalAlignment="Center"
                   Text="{Binding StatusMessage}" HorizontalAlignment="Center"/>
    </Grid>
</Window>
